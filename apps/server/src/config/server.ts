/**
 * The port the dev server binds. The local file store hands out URLs on it, so the two have to
 * agree — a mismatch serves every stored picture a 404 and nothing reports it.
 */
export const SERVER_PORT = 5173;
